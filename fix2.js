const fs = require('fs'); let content = fs.readFileSync('src/app/admin/actions.ts', 'utf8'); content += \\nexport async function testAlarm(userId: string) {\n  const session = await auth();\n  if (session?.user?.role !== 'ADMIN') throw new Error('Unauthorized');\n  const subscriptions = await prisma.pushSubscription.findMany({ where: { userId } });\n  if (subscriptions.length === 0) return { success: false, message: 'No push subscription found.' };\n  let sent = 0;\n  for (const sub of subscriptions) {\n    try {\n      await sendPushNotification(userId, { title: 'ALARM TEST', body: 'This is a test of the emergency alarm system!', url: '/' });\n      sent++;\n    } catch (e) { console.error('Push failed', e); }\n  }\n  return { success: true, message: 'Successfully sent alarm to ' + sent + ' devices.' };\n}\n\; fs.writeFileSync('src/app/admin/actions.ts', content);
